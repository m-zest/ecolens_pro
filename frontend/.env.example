# URL where the FastAPI backend is reachable.
# Local dev: http://localhost:8001
# Vercel production: leave blank — the frontend and API live on the same
# origin, and the React client will use relative /api paths.
REACT_APP_BACKEND_URL=http://localhost:8001
